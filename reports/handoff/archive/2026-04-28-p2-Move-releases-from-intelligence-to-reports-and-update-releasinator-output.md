# Implementation Plan: Move releases from intelligence to reports and update releasinator output

> **Prepared by:** Code (Gemini) (2026-04-28)
> **Assigned to:** Code
> **Repo root:** /Users/benbelanger/My Drive (ben.belanger@casebook.net)/ben-cp
> **Priority:** P2
> **STATUS**: 🔲 READY — pick up 2026-04-28

---

## Decision

`intelligence/product/releases/` (30 files) is retired. Release data belongs in `reports/` as pipeline output, not intelligence. The releasinator already generates authoritative release data from live Jira/GitHub.

## Part 1 — Delete intelligence/product/releases/

```bash
rm -rf intelligence/product/releases/
```

## Part 2 — Add per-release summary to releasinator run.py

After writing `reports/releasinator/report.md`, also write `reports/releases/YYYY-MM-DD-release.md`:

```python
release_summary_dir = os.path.join(REPO_ROOT, 'reports', 'releases')
os.makedirs(release_summary_dir, exist_ok=True)
summary_path = os.path.join(release_summary_dir, f'{release_date}-release.md')
# Write: title, date, jira_fix_version, repos_to_bump count, leaks count, link to full report
```

Adapt variable names to match what the script actually uses.

## Part 3 — Update root index.md tree

Remove `releases/` from intelligence section. Add to reports section:
```
reports/
├── releases/          — Per-release summary snapshots (generated by releasinator)
```

## Verification
- [ ] `intelligence/product/releases/` directory gone
- [ ] `reports/releases/` directory exists
- [ ] Running releasinator writes both `reports/releasinator/report.md` and `reports/releases/YYYY-MM-DD-release.md`
- [ ] Root `index.md` tree updated
- [ ] `reindex.py` runs clean with no errors
