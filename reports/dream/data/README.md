# Dream Data Directory

This directory follows the standardized repo reporting pattern.

- **raw/**: Raw sensor output JSON files and the `dream_run.json` execution log.
- **processed/**: Intermediary data used for generating the final report (if any).
- **archive/**: Snapshots of previous data runs.

The final human-readable report is located at `reports/dream/report.md`.
